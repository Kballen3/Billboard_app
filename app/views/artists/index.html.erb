<h1 class="ui header">Add an Artist</h1>
<%= link_to 'Add an Artist', new_artist_path %>
<ul>
  <% @artists.each do |artist| %>
    <li><%= link_to artist.name, artist_path(artist) %></li>
  <% end %>
</ul>